export type SpriteAssetConfig = {
  ASSET_KEY: string;
  ANIMATION_KEY: string;
  FRAME_WIDTH: number;
  FRAME_HEIGHT: number;
  STAR_FRAME: number;
  END_FRAME: number;
};

export type CharacterAssetConfig = {
  ASSET_KEY: string;
  FRAME: number;
};
